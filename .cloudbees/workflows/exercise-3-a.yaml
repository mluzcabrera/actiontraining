apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Exercise 3 A
on:
  workflow_dispatch:
    inputs:
      pathtobinary:
        type: string
        default: example.txt
        required: true
      artifactversion:
        type: string
        default: 1.0.0
        required: true
jobs:
  create_upload_register_binary:
    steps:
      - name: Create Upload and Register binary
        uses: mluzcabrera/actiontraining@main
        with:
          pathtobinary: ${{inputs.pathtobinary}}
